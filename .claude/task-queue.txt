Wire Card component into App.jsx. Find all div elements with inline card-like styles (white background, borderRadius 8-12, padding, boxShadow) and replace with <Card> from ./components/Card. Use <Card accent={colors.red}> for cards with colored borders. Import Card if not already imported. Run npm run build. Commit: "Wire Card component into App.jsx". Git push.
Wire FormInput into Admin.jsx. Every pattern of <label style={labelStyle}>...</label> followed by <input style={inputStyle} .../> should become <FormInput label="..." value={...} onChange={...} />. Import FormInput from ./components/FormInput. There are ~35 of these. Do ALL of them. Run npm run build. Commit: "Wire FormInput into Admin.jsx". Git push.
Wire FormInput into App.jsx. Same pattern as Admin.jsx — replace label+input pairs with <FormInput>. ~25 instances. Import FormInput from ./components/FormInput if not already imported. Run npm run build. Commit: "Wire FormInput into App.jsx". Git push.
Wire Modal component into Admin.jsx. Replace all inline modal overlays (fixed position divs with backdrop + centered content box) with <Modal> from ./components/Modal. Run npm run build. Commit: "Wire Modal into Admin.jsx". Git push.
Wire Modal component into App.jsx. Same pattern — replace inline modal overlays with <Modal>. Run npm run build. Commit: "Wire Modal into App.jsx". Git push.
Wire StatCard into Admin.jsx. Replace inline stat number + label box patterns with <StatCard> from ./components/StatCard. Run npm run build. Commit: "Wire StatCard into Admin.jsx". Git push.
Wire StatusBadge into Admin.jsx. Replace inline colored pill/badge spans with <StatusBadge> from ./components/StatusBadge. Run npm run build. Commit: "Wire StatusBadge into Admin.jsx". Git push.
Wire StatusBadge into App.jsx. Same pattern. Run npm run build. Commit: "Wire StatusBadge into App.jsx". Git push.
Wire Tabs into Admin.jsx. Replace inline tab button row patterns with <Tabs> from ./components/Tabs. Run npm run build. Commit: "Wire Tabs into Admin.jsx". Git push.
Wire DataTable into Admin.jsx. Replace inline sortable table patterns (including the local SortableHeader component) with <DataTable> from ./components/DataTable. Delete the local SortableHeader definition. Run npm run build. Commit: "Wire DataTable into Admin.jsx". Git push.
Wire SearchFilterBar into Admin.jsx. Replace inline search input + filter dropdown combos with <SearchFilterBar> from ./components/SearchFilterBar. Run npm run build. Commit: "Wire SearchFilterBar into Admin.jsx". Git push.
Wire SectionHeader into Admin.jsx. Replace section title + action button patterns with <SectionHeader> from ./components/SectionHeader. Run npm run build. Commit: "Wire SectionHeader into Admin.jsx". Git push.
Wire SectionHeader into App.jsx. Same pattern. Run npm run build. Commit: "Wire SectionHeader into App.jsx". Git push.
Delete any remaining duplicate theme constants in both Admin.jsx and App.jsx. Grep for hardcoded color values (#fff, #FFF, #EA2028, #231F20, #767168, #F0EDE8, #FAFAF8) and replace with colors.xxx from theme.js. Run npm run build. Commit: "Remove remaining hardcoded colors, use theme.js". Git push.
